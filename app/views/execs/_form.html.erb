<%= form_for(@exec) do |f| %>
  <% if @exec.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@exec.errors.count, "error") %> prohibited this exec from being saved:</h2>

      <ul>
      <% @exec.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
